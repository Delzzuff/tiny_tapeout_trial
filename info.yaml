# Tiny Tapeout project information
project:
  title:        "8b_approximate_multiplier"      # Project title
  author:       "AnwarM"      # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "8 bit approximate multiplier"      # One line description of what your project does
  language:     "Verilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     50000000       # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "1x1"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_Delzzuff_8b_approx_multiplier"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "tt_Delzzuff_8b_approx_multiplier.v"
    - "multiplier_wrapper.v"
    - "ks_8b.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "input for A or B"
  ui[1]: "input for A or B"
  ui[2]: "input for A or B"
  ui[3]: "input for A or B"
  ui[4]: "input for A or B"
  ui[5]: "input for A or B"
  ui[6]: "input for A or B"
  ui[7]: "input for A or B"

  # Outputs
  uo[0]: "output for MSByte or LSByte of product"
  uo[1]: "output for MSByte or LSByte of product"
  uo[2]: "output for MSByte or LSByte of product"
  uo[3]: "output for MSByte or LSByte of product"
  uo[4]: "output for MSByte or LSByte of product"
  uo[5]: "output for MSByte or LSByte of product"
  uo[6]: "output for MSByte or LSByte of product"
  uo[7]: "output for MSByte or LSByte of product"

  # Bidirectional pins
  uio[0]: "control signal for selecting A, B, LSByte of product"
  uio[1]: "control signal for selecting A, B, LSByte of product"
  uio[2]: "control signal for selecting A, B, LSByte of product"
  uio[3]: ""
  uio[4]: ""
  uio[5]: ""
  uio[6]: ""
  uio[7]: ""

# Do not change!
yaml_version: 6
